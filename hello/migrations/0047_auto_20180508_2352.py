# Generated by Django 2.0.4 on 2018-05-09 03:52

from django.db import migrations
import csv
import json

def populateMeta_db(apps, schema_editor):
    Metadata = apps.get_model('hello', 'Metadata', require_ready=True)
    metacsv = open("metadata.csv")
    metaList = csv.reader(metacsv, delimiter=',', quotechar='"')

    for row in metaList:
        newMeta = Metadata()
        newMeta.title = row[0]
        newMeta.code = row[1]
        newMeta.number_of_students = row[2]
        newMeta.trend = row[3]
        newMeta.save()

class Migration(migrations.Migration):

    dependencies = [
        ('hello', '0046_auto_20180508_2345'),
    ]

    operations = [
    migrations.RunPython(populateMeta_db)
    ]
